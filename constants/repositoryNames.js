const USERS_REPO_NAME = 'users';
const PAGES_REPO_NAME = 'pages';
const ADMINS_REPO_NAME = 'admins';
const USER_CREDS_REPO_NAME = 'user-credentials';

module.exports = {
    USERS_REPO_NAME,
    PAGES_REPO_NAME,
    USER_CREDS_REPO_NAME,
    ADMINS_REPO_NAME,
}