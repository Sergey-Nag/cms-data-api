const DEFAULT_PERMISSIONS = {
    analytics: false,
    products: false,
    orders: false,
    pages: false,
    admins: false,
    customers: false,
}

const NEW_ORDER_DESCRIPTION = 'Placed new order';

module.exports = {
    DEFAULT_PERMISSIONS,
    NEW_ORDER_DESCRIPTION,
}